{% set poster_color = poster_pic_text_color(multimediaObject) %}
{% set poster_image_url = poster_pic(multimediaObject) %}

var image = "<img src=\"{{  poster_image_url  }}\" alt=\"{{ "Event Poster" | trans }}\"";
var width = 'width="100%">';
var style = '<h2 class="warning" style="position: absolute; top: 30%; color: {{ poster_color }}; font-weight: bold; font-size: 20px; width:95%; line-height: 34px; text-align: center; word-break: break-word">';

var image = image + width;
var text = '{{ event.getNotYetHeldMessage()|default('The event has not been held yet'|trans)|raw }}';
var finalText = '<br/><span id=\"warning\"></span></h2>';

var background = image + style + text + finalText;

var nextSessionsCount = parseInt("{{ nextSessions|length }}");
var nowSessionsCount = parseInt("{{ nowSessions|length }}");
