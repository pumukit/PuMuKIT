const eventSource = new EventSource("http://localhost:8080/.well-known/mercure?topic=" + encodeURIComponent('https://livestream'));
    eventSource.onmessage = (event) => {
        $.ajax({
            url: "{{ path('pumukit_live_event_id', {"id": live.id()}) }}",
            method: 'POST',
        });
    }