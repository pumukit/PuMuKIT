{% set id = random() %}
{% set live = event.live %}

{% include "PumukitLiveBundle:Advance:player.html.twig" %}

<script type="text/javascript">
    {% include "PumukitFutureWebTVBundle:Live:Advance/Sessions/common.js.twig" %}

    if(nextSessionsCount === 0 && nowSessionsCount === 0) {
        {% include "PumukitFutureWebTVBundle:Live:Advance/Sessions/withoutSessions.js.twig" %}
    } else if (nextSessionsCount > 0 && nowSessionsCount === 0) {
        {% include "PumukitFutureWebTVBundle:Live:Advance/Sessions/futureSessions.js.twig" %}
    } else if (nowSessionsCount > 0) {
        {% include "PumukitFutureWebTVBundle:Live:Advance/Sessions/nowSessions.js.twig" %}
        {% include "PumukitLiveBundle:Advance:player.js.twig" %}
    }
</script>
