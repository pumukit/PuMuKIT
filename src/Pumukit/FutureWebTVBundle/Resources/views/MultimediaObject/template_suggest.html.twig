<div class="same-series">
    {% if multimediaObject.getSeriesTitle() %}
        {{ render(controller('PumukitFutureWebTVBundle:MultimediaObject:series', {'multimediaObject': multimediaObject, 'magicUrl': magic_url is defined ? magic_url : false})) }}
    {% endif %}
</div>

<div class="related-videos">
    {{ render(controller('PumukitFutureWebTVBundle:MultimediaObject:related', {'multimediaObject': multimediaObject})) }}
</div>
