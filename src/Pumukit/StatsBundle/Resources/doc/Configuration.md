StatsBundle configuration
=========================

Configuration:

```
pumukit_stats:
    use_aggregation: false
```

* `use_aggregation` Use ViewsAggregation instead ViewsLog for generate stats (See PumukitAggregateCommand).
