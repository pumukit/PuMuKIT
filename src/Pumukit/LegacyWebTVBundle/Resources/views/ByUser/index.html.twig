{% extends 'PumukitLegacyWebTVBundle::layout.html.twig' %}

{% block javascripts %}
    {% if scroll_list %}
        {{ parent() }}
        <script src="{{ asset('bundles/pumukitlegacywebtv/js/announces_ajax.js') }}" type="text/javascript"></script>
    {% endif %}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <br>
            <div class="row">
                <div class="col-xs-12">
                    <h1 class="title-for-crumbs">{{ title }}</h1>
                </div>
            </div>
            {% if scroll_list %}
                <div class="announces" id="announces"></div>
                <div id="announces_loading" style="display:none">
                    <img class="center-block" alt="loading" src="{{ asset('bundles/pumukitlegacywebtv/images/icons/spinner.gif') }}"/>
                </div>
                <script type="text/javascript">
                    var url_latestuploads_pager = "{{ path('pumukit_webtv_byuser_objects_pager', {'username': user.username, 'type': type}) }}";
                </script>
            {% else %}
                {% include 'PumukitLegacyWebTVBundle:ByUser:list.html.twig' %}
                <div class="row">
                    {% include 'PumukitLegacyWebTVBundle:Misc:pager.html.twig' %}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
