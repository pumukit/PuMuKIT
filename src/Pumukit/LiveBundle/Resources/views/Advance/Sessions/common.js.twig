var image = "<img src=\"{{ asset(poster(event)) }}\"";
var width = 'width="100%">';
var style = '<p class="warning" style="position: absolute; top: 40%; color: {{ poster_text_color(event) }}; font-weight: bold; font-size: 20px; width:95%; line-height: 34px; text-align: center; word-break: break-word">';

var image = image + width;
var text = '{{ event.getNotYetHeldMessage()|default('The event has not been held yet'|trans)|raw }}';
var finalText = '<br/><span id=\"warning\"></span></p>';

var background = image + style + text + finalText;

var nextSessionsCount = parseInt("{{ nextSessions|length }}");
var nowSessionsCount = parseInt("{{ nowSessions|length }}");