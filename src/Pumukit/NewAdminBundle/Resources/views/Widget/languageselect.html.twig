<li class="divider"></li>
{% for lang in languages %}
    <li class="{{ app.request.locale == lang ? 'active' }}">
      <a href="{{ path('pumukit_locale', {'locale': lang } ) }}"> {{ lang | language_name(false) }} </a>
    </li>
{% endfor %}
