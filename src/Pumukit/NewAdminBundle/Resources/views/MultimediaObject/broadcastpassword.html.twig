<span id="broadcast_password"{{ mm.getEmbeddedBroadcast() ? ((mm.getEmbeddedBroadcast().getType() == broadcast_type) ? '':' class="pumukitnewadmin-broadcast-hidden"') : ' class="pumukitnewadmin-broadcast-hidden"' }}>
  <input id="broadcast_password_1" name="broadcast_password_1" type="{{ (embeddedBroadcast.getPassword() is empty) ? 'text' : 'password' }}" placeholder="{% trans %}Introduce a password{% endtrans %}..." value="{{ (embeddedBroadcast.getPassword() is not empty) ? 'embeddedBroadcast.getPassword()' : '' }}" size="20" maxlength="256" style="margin: 5px;"/>
  <input id="broadcast_password_2" name="broadcast_password_2" type="{{ (embeddedBroadcast.getPassword() is empty) ? 'text' : 'password' }}" placeholder="{% trans %}Repeat password{% endtrans %}..." value="{{ (embeddedBroadcast.getPassword() is not empty) ? 'embeddedBroadcast.getPassword()' : '' }}" size="20" maxlength="256" />
  <input type="button" id="broadcast_password_show_hide" class="btn btn-xs" value="{% trans %}Show/Hide password{% endtrans %}" />
  <input type="button" id="broadcast_password_generate" class="btn btn-xs" title="{% trans %}Generate random password{% endtrans %}" value="{% trans %}Generate random password{% endtrans %}" />
  <span id="broadcast_password_check_fail" style="display: none; color: red; margin: 0px 10px;">{% trans %}Passwords must match{% endtrans %}.</span>
</span>
