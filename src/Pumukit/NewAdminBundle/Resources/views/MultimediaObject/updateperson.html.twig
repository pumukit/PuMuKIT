<div class="well">
  <fieldset class="">
    <dl style="margin: 0px">
      {% for role in roles %}
      <div class="form-row">
        <dt>{{ role.getName() }}{{ role.getDisplay()?'*':'' }}:</dt>
        <dd>
          <div id="{{ role.getId() }}_person_mms{{ template }}">
	        {% include 'PumukitNewAdminBundle:Person:listrelation.html.twig' with {'people': mm.getPeopleInMultimediaObjectByRole(role), 'mm': mm, 'role': role} %}
          </div>
        </dd>
      </div>
      {% endfor %}
    </dl>
  </fieldset>
</div>
