<div class="col-xs-12">
  {% dump(user) %}
  {% if user %}
    <span class="glyphicon glyphicon-user"></span> {{user.fullname?:user.username}} 
    <button type="button" class="btn btn-xs btn-danger title=" Log out">
      <span class="glyphicon glyphicon-log-out"></span>
    </button>
  {% else %}
    <button type="button" class="btn btn-default" title="Log in">
      <span class="glyphicon glyphicon-log-in"></span> Log in 
    </button>
  {% endif %}
</div>
