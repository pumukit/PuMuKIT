<div class="col-xs-12">
  {% for multimediaObject in multimediaObjects %}
    <div class="row">
      <div class="minimmobj" style="cursor:pointer;" onclick="window.location='{{ url('pumukit_webtv_multimediaobject_index', {'id': multimediaObject.getId()}) }}'">
        <div class="minithumbnail">
          <a href="{{ url('pumukit_webtv_multimediaobject_index', {'id': multimediaObject.id}) }}">
            <div class="media-object play_icon small" alt=""></div>
            <img class="media-object thumbnailimg" src="{{multimediaObject|first_url_pic()}}">
            <div class="minithumbnailholder"></div>
          </a>
        </div>
        <div class="miniinfo-wrapper">
          <div class="miniinfo">
            <a href="{{ url('pumukit_webtv_multimediaobject_index', {'id': multimediaObject.id}) }}">
              <div class="title" >
                {#{{ multimediaObject.getTitle()[:80] }}{% if (80 == (multimediaObject.getTitle()[:80]|length)) %}...{% endif %}#}
                {{ multimediaObject.getTitle()}}
              </div>
            </a>
            <div class="subtitle">
              <small>{{ multimediaObject.getSubtitle() }}</small>
            </div>
            <div class="pull-left date">
              <small>{{ multimediaObject.recordDate | localizeddate('full', 'none' , app.request.locale, null,"d/MM/Y" ) }}</small>
            </div>
          </div>
        </div>
      </div>
    </div>
  {% else %}
    {% trans %}There are no videos with these specifications{% endtrans %}.
  {% endfor %}
</div>
