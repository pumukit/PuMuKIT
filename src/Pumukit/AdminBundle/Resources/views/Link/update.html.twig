{% form_theme form 'PumukitAdminBundle:Themes:fields.html.twig' %}
{{ form(form, {'attr': {'onsubmit': 'submit_update_link(); return false;'}, 'action': path('pumukitadmin_link_update', {'id': link.id, 'mmId': mm.id})}) }}

<script type="text/javascript">
window.submit_update_link = function(){
    parent.Modalbox.hide();
    new parent.Ajax.Updater('links_mms', '{{ path('pumukitadmin_link_update', {'id': link.id, 'mmId': mm.id}) }}', {method: 'POST', asynchronous: true, evalScripts: true, parameters: Form.serialize('pumukitadmin_link')});
}
</script>
