{% if thumbnail is not empty %}
    <img src="{{ thumbnail }}" alt="{{ title }}" style="max-width:6em; height:auto; object-fit: cover;" />
{% else %}
    <img src="{{ asset(defaultImage) }}" alt="{{ title }}" style="max-width:3em; height:auto; object-fit: cover;" />
{% endif %}
