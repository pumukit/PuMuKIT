<a href="{{ url|default('#') }}" class="btn btn-primary {{ extra_class|default('') }}">
    {% if icon is defined %}
        <i class="{{ icon }} me-2"></i>
    {% endif %}
    {{ label|default('Action')|trans }}
</a>
