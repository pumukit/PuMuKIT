<button type="submit" class="btn btn-primary {{ extra_class|default('') }}">
    <i class="fa-solid {{ extra_class|default('fa-paper-plane') }} me-2"></i>
    {{ label|default('Send')|trans }}
</button>
