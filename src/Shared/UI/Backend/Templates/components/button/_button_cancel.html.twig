<a href="{{ url|default('#') }}" class="btn btn-outline-secondary {{ extra_class|default('') }}">
    <i class="fa-solid {{ icon|default('fa-xmark') }} me-2"></i>
    {{ label|default('Cancel')|trans }}
</a>
